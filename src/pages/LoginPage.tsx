import React from "react";

import FlexBox from "../components/FlexBox";

const LoginPage = () => {
  return (
    <React.Fragment>
      <FlexBox></FlexBox>
    </React.Fragment>
  );
};

export default LoginPage;
